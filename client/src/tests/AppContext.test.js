import { AppContext } from '../header/AppContext';

// This is only a template, needs implementation
it('should render correctly with AppContext', () => {
    const test = '';
 
    expect(0).toBe(0);
});
