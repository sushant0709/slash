import { NavigationDrawer } from '../header/NavigationDrawer';

// This is only a template, needs implementation
it('should render correctly with NavigationDrawer', () => {
    const test = '';
 
    expect(0).toBe(0);
});
